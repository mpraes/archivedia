import {
  GET_ONE,
  PATCH_ONE,
  DELETE_ONE,
} from "@/controllers/spaces.controller";

export const dynamic = "force-dynamic";

export const GET = GET_ONE;
export const PATCH = PATCH_ONE;
export const DELETE = DELETE_ONE;
